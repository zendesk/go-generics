# Test Module

This module is used internally in go-generics for its own test suite. You may use it as well if desired. This design prevents any transitive dependencies on other 
test frameworks and keeps the go.mod file clean.